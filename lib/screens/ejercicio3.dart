/*"Determinar la presión en un líquido, dada la profundidad, densidad del fluido y gravedad."

🔢 Variables a pedir:

Profundidad (metros).
Densidad del fluido (kg/m³, opcional: agua = 1000 kg/m³).
Gravedad (opcional).
📐 Fórmula: P = densidad * gravedad * profundidad

⚠️ Si la velocidad es negativa , mostrar en un alert: *La profundidad no puede ser negativa* */

import 'package:flutter/material.dart';

class Ejercicio3 extends StatelessWidget {
  const Ejercicio3({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}